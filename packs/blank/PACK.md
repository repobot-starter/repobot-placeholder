# Pack: blank (no template)

Default pack when the user picks **no template**. Public landing only — logo and a prompt to chat with the agent.

## What ships

- Centered landing at `/` (`web/app/src/View/Blank/`)
- No domain GraphQL contract beyond the kernel exemplars (`/login`, `/users`, `/projects`)

## Agent recipe

Treat this as a blank canvas. Keep the landing until the product has a real home, then replace `/` with the app shell the user asked for. Use Identity/Project exemplars as pattern references, not as the product UI.
