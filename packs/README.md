# Template packs

Vertical overlays for `repobot-base`. Each pack lives under `packs/<key>/` with catalog metadata (`catalog.json`) and an agent recipe (`PACK.md`). Packs are the top tier of the repo's reuse model — they own only their vertical's content and compose the kernels below them (see `docs/kernels.md` for the kernel/chassis/util/pack rubric).

| Key                                 | Template key                                | Meaning                                                                                                                                                                                                                                                                                                          |
| ----------------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `blank`                             | `repobot-placeholder`                       | **No template** — simple landing; default when the user picks no example                                                                                                                                                                                                                                         |
| `paint`                             | `repobot-paint`                             | Retro paint studio (client-only canvas app)                                                                                                                                                                                                                                                                      |
| `pong`                              | `repobot-pong`                              | Classic pong cabinet (client-only canvas game)                                                                                                                                                                                                                                                                   |
| `snake`                             | `repobot-snake`                             | Terminal snake (client-only canvas game)                                                                                                                                                                                                                                                                         |
| `astro`                             | `repobot-astro`                             | Neon space shooter (client-only canvas game)                                                                                                                                                                                                                                                                     |
| `asteroid`                          | `repobot-asteroid`                          | Vector asteroids: thrust, drift, split the rocks (client-only canvas game)                                                                                                                                                                                                                                       |
| `blackjack`                         | `repobot-blackjack`                         | Casino blackjack vs a dealer bot (client-only card game)                                                                                                                                                                                                                                                         |
| `chess`                             | `repobot-chess`                             | Full-rules chess vs a bot or a friend (client-only board game)                                                                                                                                                                                                                                                   |
| `style`                             | `repobot-style`                             | Dress-to-impress styling game on a runway timer (client-only)                                                                                                                                                                                                                                                    |
| `cabin`                             | `repobot-cabin`                             | Flight-attendant cabin sim: serve snacks, calm passengers (client-only)                                                                                                                                                                                                                                          |
| `salon`                             | `repobot-salon`                             | Hair salon glow-up: wash, cut, color, style (client-only)                                                                                                                                                                                                                                                        |
| `sitter`                            | `repobot-sitter`                            | Babysitting chaos: fix mishaps before the parents get home (client-only)                                                                                                                                                                                                                                         |
| `code`                              | `repobot-code`                              | Block-programming puzzles: guide a robot through obstacle courses (client-only)                                                                                                                                                                                                                                  |
| `ludo`                              | `repobot-ludo`                              | Classic four-color ludo race vs bots or hotseat friends (client-only)                                                                                                                                                                                                                                            |
| `gomoku`                            | `repobot-gomoku`                            | Five-in-a-row on a 15x15 board vs a three-level bot (client-only)                                                                                                                                                                                                                                                |
| `tawla`                             | `repobot-tawla`                             | Backgammon: race the pips, hit the blots, bear off first (client-only)                                                                                                                                                                                                                                           |
| `carrom`                            | `repobot-carrom`                            | Carrom striker board: flick, pocket, cover the queen (client-only)                                                                                                                                                                                                                                               |
| `hanafuda`                          | `repobot-hanafuda`                          | Koi-Koi with the 48-card hanafuda flower deck (client-only)                                                                                                                                                                                                                                                      |
| `truco`                             | `repobot-truco`                             | Brazilian truco: manilhas, bluffs, and raise ladders (client-only)                                                                                                                                                                                                                                               |
| `race`                              | `repobot-race`                              | Neon highway racer: lane changes, nitro, overtake bonuses (client-only)                                                                                                                                                                                                                                          |
| `chimney`                           | `repobot-chimney`                           | Rooftop runner: jump house by house, land in a chimney and you get cooked (client-only)                                                                                                                                                                                                                          |
| `link`                              | `repobot-link`                              | Link-in-bio page: profile, links, socials, theme palettes (client-only)                                                                                                                                                                                                                                          |
| `folio`                             | `repobot-folio`                             | Editorial portfolio: hero statement, filterable projects, contact (client-only)                                                                                                                                                                                                                                  |
| `resume`                            | `repobot-resume`                            | Personal résumé / CV one-pager: computed durations, print-to-PDF one-pager, links band (client-only)                                                                                                                                                                                                             |
| `resume-yara`                       | `repobot-resume-yara`                       | **Derived** from `resume`: chef résumé — Yara Haddad, chef de cuisine in Portland, Maine; photographed seed (full-bleed hero, kitchens on a photographic timeline, work as stories), `galley` register                                                                                                           |
| `resume-dev`                        | `repobot-resume-dev`                        | **Derived** from `resume`: developer résumé — staff-engineer content seed, true-black mono register (monolith), achromatic white brand                                                                                                                                                                           |
| `influencer-lua`                    | `repobot-influencer-lua`                    | **Derived** from `influencer`: Lua Marinho, a Lisbon fashion & slow-travel creator — `miradouro` register, the home `edition`: the diary as a full-bleed stack captioned inside its photographs, looks on a portrait rail                                                                                        |
| `influencer-remi`                   | `repobot-influencer-remi`                   | **Derived** from `influencer`: Remi Adebayo, a Peckham film & streetwear creator — `sprocket` register, the home `edition`: his name as the masthead, the week's roll as a contact sheet, the fits as numbered plates                                                                                            |
| `photography`                       | `repobot-photography`                       | Photography portfolio: full-bleed hero, justified galleries + lightbox, albums, inquiry (client-only)                                                                                                                                                                                                            |
| `photography-music`                 | `repobot-photography-music`                 | Music photographer portfolio: full-page hero reel, sequenced live/festival/studio albums, booking form (client-only)                                                                                                                                                                                             |
| `photography-family`                | `repobot-photography-family`                | Family, motherhood & newborn photographer: `heirloom` light-and-airy register, crossfade hero, justified galleries, booking, proofing (client-only)                                                                                                                                                              |
| `photography-wren`                  | `repobot-photography-wren`                  | **Derived** from `photography`: Wren Hollis, a Los Angeles portrait photographer — `vitrine` stone register, one same-size stack on caption bands                                                                                                                                                                |
| `photography-kaito`                 | `repobot-photography-kaito`                 | **Derived** from `photography`: Kaito Mori, a New York headshot photographer — `seamless` white register, faces on colored seamless, one same-size stack                                                                                                                                                         |
| `photography-music-theo`            | `repobot-photography-music-theo`            | **Derived** from `photography-music`: Theo Marsh, an East Nashville music photographer — `liner` near-black register, a record wall of square album covers                                                                                                                                                       |
| `photography-family-adalin`         | `repobot-photography-family-adalin`         | **Derived** from `photography-family`: Ada Lin, a Seattle newborn and motherhood photographer — `milkglass` daylight-studio register, one full-bleed window photograph over a session builder (a pick-one session, keepsakes and extras as photograph tiles beside a running total), client-only                 |
| `photography-family-ashgrove`       | `repobot-photography-family-ashgrove`       | **Derived** from `photography-family`: Ashgrove Portrait Studio, a Hudson NY heritage portrait studio — `darkroom` charcoal-and-silver register, one full-bleed black-and-white plate over the prints (one sitting in four processes, matted like museum prints with engraved plaques), client-only              |
| `photography-family-documentary`    | `repobot-photography-family-documentary`    | **Derived** from `photography-family`: Hattie Moreau, a Chicago documentary family photographer — its own seed, imagery, the pinboard home, and `snapshot`                                                                                                                                                       |
| `wedding`                           | `repobot-wedding`                           | Wedding photography studio: sequenced wedding galleries, flat-priced packages + FAQ, client proofing (client-only)                                                                                                                                                                                               |
| `wedding-edition`                   | `repobot-wedding-edition`                   | Tabloid wedding photographer (The Late Edition): front-page hero, news stories, contact-sheet galleries, client proofing (client-only)                                                                                                                                                                           |
| `wedding-fiorella`                  | `repobot-wedding-fiorella`                  | **Derived** from `wedding`: Fiorella Conti, a film wedding photographer on the Amalfi Coast, Lake Como and in Puglia — `limone` Italian-coast register, wide-capital wordmark masthead, captioned full-bleed stack home, contact-sheet weddings with grease-pencil picks, collections in euros (client-only)     |
| `wedding-highline`                  | `repobot-wedding-highline`                  | **Derived** from `wedding`: Highline, adventure elopements out of Telluride — `alpine` expedition field-sheet register, wide heavy wordmark, the places as a captioned full-bleed stack with seasons and coordinates, a spec sheet of permits and elevations, every elopement as a captioned stack (client-only) |
| `services`                          | `repobot-services`                          | Local service business: services + prices, draggable before/after projects, live open badge, quote form (client-only)                                                                                                                                                                                            |
| `services-emergency`                | `repobot-services-emergency`                | Emergency service business: call-first hero, dispatch metrics, flat printed prices, service-request form (client-only)                                                                                                                                                                                           |
| `services-recurring`                | `repobot-services-recurring`                | Recurring service business: plans on the home page, what's-included comparison, live open badge, booking form (client-only)                                                                                                                                                                                      |
| `services-landscape`                | `repobot-services-landscape`                | **Derived** from `services`: landscaping company — its own content seed, imagery, and moss-green brand                                                                                                                                                                                                           |
| `services-painting`                 | `repobot-services-painting`                 | **Derived** from `services`: painting company — its own content seed, imagery, and plum brand                                                                                                                                                                                                                    |
| `services-hair`                     | `repobot-services-hair`                     | **Derived** from `services`: one-chair hair color studio — its own content seed, imagery, and rosewood brand                                                                                                                                                                                                     |
| `services-makeup`                   | `repobot-services-makeup`                   | **Derived** from `services`: bridal & occasion makeup artist — its own content seed, imagery, and mulberry brand                                                                                                                                                                                                 |
| `services-builder`                  | `repobot-services-builder`                  | **Derived** from `services`: Tideline Builders, a Cannon Beach custom builder — its own seed, imagery, build log, materials board, and `tideline`                                                                                                                                                                |
| `services-landscape-native`         | `repobot-services-landscape-native`         | **Derived** from `services`: Wild Ground, an Austin native-plant studio — its own seed, riso-printed imagery, and the `riso` register                                                                                                                                                                            |
| `services-painting-casacal`         | `repobot-services-painting-casacal`         | **Derived** from `services`: Casa Cal, a Santa Barbara lime-plaster studio — `limewash` register, the `stack` home of rooms captioned inside their photographs                                                                                                                                                   |
| `services-painting-swiss`           | `repobot-services-painting-swiss`           | **Derived** from `services`: Shotgun Color Co., a New Orleans house painter — its own seed, imagery, a paint-chip deck, and `paintchip`                                                                                                                                                                          |
| `services-hair-braids`              | `repobot-services-hair-braids`              | **Derived** from `services`: The Crown Room, a Bed-Stuy braids & natural hair studio — its own seed, imagery, cover lines, style menu, lookbook, and `crown`                                                                                                                                                     |
| `services-makeup-counter`           | `repobot-services-makeup-counter`           | **Derived** from `services`: Marisol Vega, a Los Angeles editorial & bridal makeup artist — its own seed, imagery, shade cards, rate card, and `vanity`                                                                                                                                                          |
| `services-landscape-olivetta`       | `repobot-services-landscape-olivetta`       | **Derived** from `services`: Olivetta Garden Studio, Montecito — its own seed, imagery, the seasonal garden journal, and `plaster`                                                                                                                                                                               |
| `services-landscape-koen`           | `repobot-services-landscape-koen`           | **Derived** from `services`: Kōen Garden Studio, Bainbridge Island — its own seed, imagery, a numbered garden walk, the seal, and `basalt`                                                                                                                                                                       |
| `services-makeup-tidewater`         | `repobot-services-makeup-tidewater`         | **Derived** from `services`: Tidewater Beauty Co., a Charleston on-location bridal team — its own seed, imagery, a photographic morning timeline, and `daybook`                                                                                                                                                  |
| `services-makeup-noor`              | `repobot-services-makeup-noor`              | **Derived** from `services`: Studio Noor, an Edison NJ South Asian bridal studio — its own seed, imagery, arched event panels, a package builder, and `jharokha`                                                                                                                                                 |
| `services-contractor-meridian`      | `repobot-services-contractor-meridian`      | **Derived** from `services`: Meridian Coastal, a Malibu luxury coastal builder — its own seed, imagery, a wide masthead over a panorama, a residences rail, a design story, and `horizon`                                                                                                                        |
| `services-contractor-hallock`       | `repobot-services-contractor-hallock`       | **Derived** from `services`: Hallock & Sons, an Amagansett shingle-style estate builder — its own seed, imagery, one estate told as a long-form feature, and `shingle`                                                                                                                                           |
| `services-hair-maren`               | `repobot-services-hair-maren`               | **Derived** from `services`: Maren Atelier, a Dallas private-suite hair atelier — its own seed, imagery, the stylists as a directory by level, and `limestone`                                                                                                                                                   |
| `services-hair-sable`               | `repobot-services-hair-sable`               | **Derived** from `services`: Sable Atelier, a Washington DC natural-hair and silk-press studio — its own seed, imagery, the ritual as a timed photographed strip, and `cognac`                                                                                                                                   |
| `services-electric`                 | `repobot-services-electric`                 | **Derived** from `services-emergency`: electrician — its own content seed, imagery, and amber brand                                                                                                                                                                                                              |
| `services-hvac`                     | `repobot-services-hvac`                     | **Derived** from `services-emergency`: HVAC company — its own content seed, imagery, and steel-teal brand                                                                                                                                                                                                        |
| `services-emergency-van`            | `repobot-services-emergency-van`            | **Derived** from `services-emergency`: Straight Pipe Plumbing, Pasadena — `jacaranda` register, price board on a full-bleed hero, job report, text-thread steps                                                                                                                                                  |
| `services-electric-techno`          | `repobot-services-electric-techno`          | **Derived** from `services-emergency`: Live Wire Electric, Detroit — `schematic` register, full-bleed hero, ticker readout, job report                                                                                                                                                                           |
| `services-hvac-desert`              | `repobot-services-hvac-desert`              | **Derived** from `services-emergency`: 115 Degrees, Phoenix — `sunbelt` register, 115° readout beside the photo, price tags                                                                                                                                                                                      |
| `services-emergency-hartwell`       | `repobot-services-emergency-hartwell`       | **Derived** from `services-emergency`: Hartwell Mechanical, Greenwich estates — `plantroom` register, full-bleed hero, the systems index                                                                                                                                                                         |
| `services-emergency-bayou`          | `repobot-services-emergency-bayou`          | **Derived** from `services-emergency`: Bayou Restoration, Houston — `stormline` register, intake card over the storm photo, dispatch board, before/after                                                                                                                                                         |
| `services-lawncare`                 | `repobot-services-lawncare`                 | **Derived** from `services-recurring`: lawn care company — its own content seed, imagery, and deep-emerald brand                                                                                                                                                                                                 |
| `services-pest`                     | `repobot-services-pest`                     | **Derived** from `services-recurring`: pest control company — its own content seed, imagery, and brick-red brand                                                                                                                                                                                                 |
| `services-recurring-turnover`       | `repobot-services-recurring-turnover`       | **Derived** from `services-recurring`: Clean Getaway, Miami turnover crew — `memphis` register, trading-card prices, turnover rail, door-hanger checklist                                                                                                                                                        |
| `services-lawncare-crew`            | `repobot-services-lawncare-crew`            | **Derived** from `services-recurring`: Fresh Cut, Atlanta lawn crew — `gameday` register, season-ticket plans, volt brand                                                                                                                                                                                        |
| `services-pest-creature`            | `repobot-services-pest-creature`            | **Derived** from `services-recurring`: Night Crawlers, Houston pest crew — `creature` register, creature index, orange brand                                                                                                                                                                                     |
| `services-recurring-linen`          | `repobot-services-recurring-linen`          | **Derived** from `services-recurring`: Linen & Laurel, Beverly Hills housekeeping — `whiteglove` register, the house-manager text thread, black brand                                                                                                                                                            |
| `services-recurring-worth`          | `repobot-services-recurring-worth`          | **Derived** from `services-recurring`: The Worth Pool Co., Palm Beach — `palmbeach` register, the season calendar, bamboo frame, palm-green brand                                                                                                                                                                |
| `care`                              | `repobot-care`                              | Primary-care practice (Alder House Family Medicine, Bellingham): `luxe-light` in calm teal, insurance strip, provider portraits, real appointment booking                                                                                                                                                        |
| `care-therapy`                      | `repobot-care-therapy`                      | **Derived** from `care`: Stillwater Family Therapy, Madison — its own seed and imagery, warm clay brand over `luxe-light`, centered masthead                                                                                                                                                                     |
| `care-psychology`                   | `repobot-care-psychology`                   | **Derived** from `care`: Juniper Hill Psychology, Ann Arbor — its own seed and imagery, slate-blue brand over `luxe-light`, split letterhead                                                                                                                                                                     |
| `care-dental`                       | `repobot-care-dental`                       | **Derived** from `care`: Waverly Dental, Madison — its own seed and imagery, sky-blue brand over `luxe-light`, inline nav card                                                                                                                                                                                   |
| `care-direct`                       | `repobot-care-direct`                       | **Derived** from `care`: Dr. Ada Okafor Direct Primary Care — `wayfinding` transit-sign register, price list, house calls                                                                                                                                                                                        |
| `care-therapy-duet`                 | `repobot-care-therapy-duet`                 | **Derived** from `care`: Duet Family Therapy, Silver Lake — `groove` LP-sleeve register, Side A / Side B session tracklist                                                                                                                                                                                       |
| `care-psychology-studio`            | `repobot-care-psychology-studio`            | **Derived** from `care`: Miriam Castellanos, PsyD — `inkblot` gallery-poster register, inkblot plates, wall-label hero                                                                                                                                                                                           |
| `care-dental-bright`                | `repobot-care-dental-bright`                | **Derived** from `care`: Bright Side Dental, Koreatown — `bubblegum` candy-pop register, sticker badges, glossy pills                                                                                                                                                                                            |
| `care-primary-magnolia`             | `repobot-care-primary-magnolia`             | **Derived** from `care`: Magnolia Concierge Medicine, Green Hills — `parlor` register, the first year as a photographic timeline, membership price card                                                                                                                                                          |
| `care-therapy-opentrail`            | `repobot-care-therapy-opentrail`            | **Derived** from `care`: Open Trail Counseling, Boulder — `trailhead` register, walk-and-talk sessions, the therapists' field notes with pencil trail maps                                                                                                                                                       |
| `care-therapy-kindred`              | `repobot-care-therapy-kindred`              | **Derived** from `care`: Kindred Therapy Collective, Atlanta — `hearth` register, culturally affirming therapy, a filterable therapist directory and how matching works                                                                                                                                          |
| `care-psychology-cambridge`         | `repobot-care-psychology-cambridge`         | **Derived** from `care`: Cambridge Neuropsychology, Cambridge MA — `colophon` register, assessment for ADHD, autism, learning and memory, an annotated sample report then testing day, feedback and the report                                                                                                   |
| `care-psychology-northlight`        | `repobot-care-psychology-northlight`        | **Derived** from `care`: Northlight Psychiatry, Minneapolis — `boreal` register, medication management in person and by telehealth, the first 90 days as one horizontal track                                                                                                                                    |
| `care-dental-atwater`               | `repobot-care-dental-atwater`               | **Derived** from `care`: Atwater Orthodontics, Gold Coast — `terrazzo` lakeside register, full-bleed hero, before/after smiles, a pictogram path                                                                                                                                                                 |
| `care-dental-fernfinch`             | `repobot-care-dental-fernfinch`             | **Derived** from `care`: Fern & Finch Pediatric Dentistry, Austin — `picturebook` storybook register, full-bleed hero, the first visit as five gouache pages                                                                                                                                                     |
| `care-obgyn-solana`                 | `repobot-care-obgyn-solana`                 | **Derived** from `care`: Solana Women's Health, Santa Monica — `seaglass` ocean-view register, full-bleed hero, five life-stage cards under arched photographs                                                                                                                                                   |
| `care-obgyn-oakadobe`               | `repobot-care-obgyn-oakadobe`               | **Derived** from `care`: Oak & Adobe Birth Center, Ojai — `adobe` register, the `stack` home between olive and fired-clay bands, two midwives                                                                                                                                                                    |
| `care-obgyn-clearwater`             | `repobot-care-obgyn-clearwater`             | **Derived** from `care`: Clearwater Fertility, San Francisco — `fogline` register, the `stack` home as a photograph, the numbers report, and the prices                                                                                                                                                          |
| `band`                              | `repobot-band`                              | Band/artist site: computed tour dates + badge, waveform demo players, video, mailing list, downloadable EPK (client-only)                                                                                                                                                                                        |
| `dj`                                | `repobot-dj`                                | DJ / electronic artist: numbered mix shelf with waveform players, computed set dates, tech-rider booking form (client-only)                                                                                                                                                                                      |
| `single`                            | `repobot-single`                            | Release one-pager: countdown to the release date that flips to OUT NOW, excerpt player, tracklist, pre-save links (client-only)                                                                                                                                                                                  |
| `estate`                            | `repobot-estate`                            | Real-estate agent site: listings grid with computed status badges, neighborhood guides, agent bio, inquiry form (client-only)                                                                                                                                                                                    |
| `estate-aldercott`                  | `repobot-estate-aldercott`                  | **Derived** from `estate`: Aldercott, a Bellingham waterfront broker — `baylight` register, the `stack` home of listings as full-bleed plates captioned with place, price, and status                                                                                                                            |
| `interiors-architecture-aokifarrow` | `repobot-interiors-architecture-aokifarrow` | **Derived** from `interiors`: Aoki Farrow, a New York architecture practice — `schist` register, the `monograph` home of numbered full-bleed plates, a feature, and a ruled index of works                                                                                                                       |
| `fund-index`                        | `repobot-fund-index`                        | Venture fund site, index register: dark mono, numbered focus areas + principles, metrics band, computed numerals (client-only)                                                                                                                                                                                   |
| `fund-index-stet`                   | `repobot-fund-index-stet`                   | **Derived** from `fund-index`: Stet, a pre-seed fund in New York — `galleyproof` register, the one-line home: the thesis at poster scale, the portfolio as a run of names, one closing line                                                                                                                      |
| `church`                            | `repobot-church`                            | Church site: computed next-service badge, auto-split events calendar, ministries, sermon archive, plan-a-visit form (client-only)                                                                                                                                                                                |
| `nonprofit`                         | `repobot-nonprofit`                         | Nonprofit site: impact-numbers typography, program spreads, auto-split volunteer calendar, signup form, external Donate CTA (client-only)                                                                                                                                                                        |
| `community`                         | `repobot-community`                         | Neighborhood association site: auto-split events calendar with next-up badge, membership dues tiers, join form, board roster (client-only)                                                                                                                                                                       |
| `fitness`                           | `repobot-fitness`                           | Strength studio site: live weekly schedule grid + next-class badge, coach bios, pricing tiers, free-week form (client-only)                                                                                                                                                                                      |
| `fitness-yoga`                      | `repobot-fitness-yoga`                      | Yoga & pilates studio site: daily schedule grid + live badge, teacher portraits, mat/reformer tiers, intro offer (client-only)                                                                                                                                                                                   |
| `fitness-trainer`                   | `repobot-fitness-trainer`                   | Personal trainer site: live training-week rows + next-session badge, coach bio, program tiers, free-consult form (client-only)                                                                                                                                                                                   |
| `trade`                             | `repobot-trade`                             | Supply-chain business site: commodities, journey timeline, shipment board (client-only)                                                                                                                                                                                                                          |
| `launch`                            | `repobot-launch`                            | Startup landing page: waitlist, features, pricing, FAQ (client-only)                                                                                                                                                                                                                                             |
| `blog`                              | `repobot-blog`                              | Markdown blog: logo-only masthead, typography-first post list, article reader (client-only)                                                                                                                                                                                                                      |
| `menu`                              | `repobot-menu`                              | Café site: menu with dietary filters, live open/closed badge, contact (client-only)                                                                                                                                                                                                                              |
| `flash`                             | `repobot-flash`                             | Flashcards with a five-box spaced-repetition scheduler, progress on device (client-only)                                                                                                                                                                                                                         |
| `quiz`                              | `repobot-quiz`                              | Multiple-choice trivia: instant feedback, explained misses, best scores (client-only)                                                                                                                                                                                                                            |
| `sugar`                             | `repobot-sugar`                             | Physical retail landing page: rotating daily menu, live machine statuses (client-only)                                                                                                                                                                                                                           |
| `auth`                              | `repobot-auth`                              | User accounts on the kernel Identity domain (full-stack; built-in auth on deploy)                                                                                                                                                                                                                                |
| `saas`                              | `repobot-saas`                              | Outlay, team spend management: launch-grade landing + pricing, auth, spend dashboard (full-stack)                                                                                                                                                                                                                |
| `shop`                              | `repobot-shop`                              | Single-product storefront with Stripe checkout, no accounts (full-stack)                                                                                                                                                                                                                                         |
| `chat`                              | `repobot-chat`                              | AI chat thread with streaming answers, reasoning, and tool calls (full-stack; web + iOS)                                                                                                                                                                                                                         |
| `talk`                              | `repobot-talk`                              | Push-to-talk AI voice assistant on iOS via OpenAI Realtime (full-stack)                                                                                                                                                                                                                                          |
| `invoice`                           | `repobot-invoice`                           | Invoice generator: form in, professional PDF out via the documents kernel (full-stack)                                                                                                                                                                                                                           |
| `pdf`                               | `repobot-pdf`                               | **Feature**: schema-driven PDF generator over every document template the repo ships (full-stack)                                                                                                                                                                                                                |
| `checkout`                          | `repobot-checkout`                          | **Feature**: one product, one buy button, a real Stripe checkout (full-stack)                                                                                                                                                                                                                                    |
| `interpret`                         | `repobot-interpret`                         | **Feature**: drop a PDF, get an AI interpretation — type, summary, key points, fields (full-stack)                                                                                                                                                                                                               |
| `entry`                             | `repobot-entry`                             | **Feature**: define fields, enter records, export CSV; on-device until wired to a database (client-only)                                                                                                                                                                                                         |
| `quickbooks`                        | `repobot-quickbooks`                        | **Feature**: QuickBooks connect + books on one page; sample company in sandbox, real Intuit on deploy (full-stack)                                                                                                                                                                                               |
| `agent`                             | `repobot-agent`                             | **Feature**: talk or chat with an agent that lives on a songs catalog — ChatGPT for the mind, ElevenLabs for the voice                                                                                                                                                                                           |
| `accounting`                        | `repobot-accounting`                        | AI accounting advisor: QuickBooks dashboard, invoice/customer tables, advisor chat (full-stack)                                                                                                                                                                                                                  |
| `cfo`                               | `repobot-cfo`                               | Fractional CFO practice portal: client invites, per-client books, statement drilldowns, XLSX (full-stack)                                                                                                                                                                                                        |
| `credit`                            | `repobot-credit`                            | Letter-of-credit checking desk: drop the LC + documents, get the discrepancy report (full-stack)                                                                                                                                                                                                                 |
| `flow`                              | `repobot-flow`                              | Live budgeting: linked grids over live accounting data, always-current variance, XLSX both ways (full-stack)                                                                                                                                                                                                     |
| `pitch`                             | `repobot-pitch`                             | Investor deck builder: slides filled from live books, brand controls, print-quality PDF export (full-stack)                                                                                                                                                                                                      |

[`active.json`](active.json) records which pack owns `/` in this checkout. Kernel exemplar routes (`/login`, `/users`, `/projects`) always remain for agents; inactive packs keep their preview routes (`/paint`, `/pong`, `/snake`, `/astro`, `/asteroid`, `/blackjack`, `/chess`, `/style`, `/cabin`, `/salon`, `/sitter`, `/code`, `/ludo`, `/gomoku`, `/tawla`, `/carrom`, `/hanafuda`, `/truco`, `/race`, `/chimney`, `/link`, `/folio`, `/resume`, `/photography`, `/wedding`, `/services`, `/emergency`, `/cleaning`, `/trade`, `/launch`, `/blog`, `/menu`, `/flash`, `/quiz`, `/sugar`, `/shop`, `/chat`, `/talk`, `/invoice`, `/pdf`, `/checkout`, `/interpret`, `/entry`, `/quickbooks`, `/agent`).

## catalog.json contract

- `key` — pack key; matches the folder and `active.json`.
- `templateKey` — the platform's template id: the GitHub template repo name and the project picker key.
- `base` — required: the base-template family this pack belongs to, one of `landing | app | store | game | content`. The platform's workspace groups projects around ~4 configurable bases (landing page, web app, store, game) and presents complete vertical starters separately; `base` names the family a pack starts from — the physical clustering that already exists (all `View/Site` packs share the full-stack app chassis → `app`; everything under `View/Games/` → `game`; `launch` → `landing`; `shop` → `store`; content-first singletons like blog/folio/link/menu/quiz/flash and the AI chat/voice starters → `content`). **The vocabulary is append-only**: the platform's `TemplateRegistry` mirrors it in lockstep, so never rename or remove a value — only add one (and add it on the platform side in the same motion). Enforced by `scripts/verify-pack-catalogs.mjs` (check-all) and `compose-pack.test.mjs`; compose emits it into `repobot.deploy.json` so the platform can place a project without re-reading the pack catalog. Judgment calls on record: `blank` is `app` (the placeholder's simple landing page is incidental chrome — "no template" means "build from scratch", and the general-purpose configurable base is the app one; no `blank`/`none` value was minted for a single pack), `auth` and `invoice` are `app` (users experience them as applications — an accounts scaffold and a form-to-PDF tool), `sugar` and `trade` are `landing` (conversion-first marketing pages: host-a-machine and quote CTAs) while `menu` stays `content` (informational site for existing customers — hours, menu, directions — not a conversion page).
- `isFeature` — optional, `true` on **feature packs**: one-page feature apps (PDF generator, Stripe checkout, AI document interpreter, data entry, QuickBooks sync) that work standing alone as a project AND can be added into an existing project. The platform's add-a-feature flow unions the pack's `capabilities` into the project's `repobot.deploy.json` and exposes its preview route as a page, without switching the active pack — which works because every composed project already carries every pack's views at their preview routes. Must be exactly `true` when present (omission, not `false`, is the "not a feature" signal), mirroring `isBase`; the platform's `TemplateRegistry` carries the same flag. Feature packs are exempt from the compose-ready approval bar (their product is the tool, not a composable marketing site — same reasoning as games). Compose emits it into `repobot.deploy.json`.
- `isBase` — optional, `true` only on the canonical configurable base of a family: `launch` (landing), `saas` (app), `shop` (store). Every other pack omits it (omission, not `false`, is the "starter project" signal). The game family deliberately has no `isBase` pack: the workspace's game base tile is a picker across the game packs, so no single game is the canonical starting point.
- `category` — optional: the marketing-site category this pack starts (`photography`, `services`, and coming: `weddings-events`, ...). Category starters are the successor naming scheme to the bot-persona templates: the first starter in a category takes the bare category key as its pack key; later alternates get suffixed keys and share the `category` value so pickers group them. Append-only vocabulary, mirrored by the platform like `base`.
- `clientOnly` — `true` means deploys ship the built web app statically and skip functions, migrations, and database provisioning until the manifest changes.
- `capabilities` — provisioned-infrastructure badges (`AUTH`, `DATABASE`, `EMAIL`, `BACKEND`, `PAYMENTS`, `AI`, `DOCUMENTS`, `STORAGE`, `JOBS`) shown in the picker and read by the platform to decide what to provision and inject (`PAYMENTS` adds the deploy-time `STRIPE_SECRET_KEY` from the account's connected Stripe integration; `AI` adds the deploy-time AI gateway credentials — keyless, billed to the account's platform credits — or the account's own `OPENAI_API_KEY` when an OpenAI integration is connected; `DOCUMENTS` adds the deploy-time `DOCUMENTS_RENDER_URL` + `DOCUMENTS_TOKEN` for the platform's PDF render service; `STORAGE` adds the deploy-time `STORAGE_MODE=gcs` + `STORAGE_BUCKET`/`STORAGE_PREFIX` for the environment's object storage; `JOBS` provisions one Cloud Scheduler job per environment and adds the deploy-time `JOBS_MODE=scheduler` + `JOBS_TOKEN` so the jobs registry ticks on a real schedule).
- `authMethods` — optional, packs with `AUTH` only: the sign-in methods this template ships (see `docs/auth.md`), defaulting to `["email-code"]`. Compose emits it into `repobot.deploy.json` (driving the platform's auth provisioning and the deploy-time `VITE_AUTH_METHODS`) and stamps `AUTH_METHODS` into the native config files.
- `contentContract` — optional: the pack's content slots as data, for design-space composers (agents and platform tooling that generate variants of the pack rather than hand-editing it). `module` names the pack's typed content file; `slots` maps slot paths to `{ kind, min?, optional?, item? }` where `kind` is one of `text | text[] | number | media | media[] | collection` (collections nest an `item` slot map; `number` is a bare numeric value — a price, a count — that a composer must bind as a number, never prose; `optional: true` on a `text` or `media` slot lets a module leave it unbound — the key present as `null` so the editor can fill it — e.g. a hero photo a derived template shows and the base doesn't). The contract is authored alongside the content module — the module satisfies it, the pack's content tests guard it, and it never drifts ahead of what the module actually renders. Declaring one (together with `landing` seeds for every routed page) is what makes a pack **compose-ready**: `scripts/generate-design-manifest.mjs` computes the flag and publishes the pack's block in `docs/design-manifest.json`; `scripts/verify-pack-catalogs.mjs` validates the shape. `photography` is the reference implementation. New packs should ship one from day one; existing packs join only when promoted.

Packs that sell things declare the `PAYMENTS` capability and reuse the kernel payments domain (`paymentsService` checkout sessions, the Stripe webhook, the `purchases` ledger, and session-gated delivery) — never bespoke Stripe wiring. A pack owns only its server-side product catalog and a thin create mutation, mirroring the shop exemplar; prices never come from the client: see `docs/payments.md`. In the sandbox checkout is simulated for free (`PAYMENTS_MODE=local`); deploys get real Stripe Checkout via the account's connected Stripe integration.

Packs that need user accounts declare the `AUTH` capability and reuse the kernel auth component (`AuthCard`/`AuthScreen` behind `ProtectedRoutes` and the `/login` route; native `SignInView` twins) — never a bespoke login form. Enabled methods and branding are configuration, not new UI: see `docs/auth.md`.

Packs (and upgrades) that want an assistant declare the `AI` capability and reuse the kernel chat surface (`AiChatThread` over `useAiChat` on web; the `AiChatStore`/`AiChatComponent` twins on iOS) — never bespoke streaming plumbing. Voice packs likewise compose the kernel voice broker (`AiVoiceService` on the backend; the `AiVoiceStore`/`AiVoiceComponent` twins on iOS), never their own Realtime wiring. Web hold-to-talk (the `agent` pack) composes `ai__request__voice` over ElevenLabs STT/TTS around the same chat brain. Customization is the system prompt, the voice instructions, and domain tools in `AiChatTools.ts`: see `docs/ai.md`. In the sandbox both chat and voice are simulated for free (`AI_MODE=local` / `ELEVENLABS_MODE=local`); deploys get the real models automatically — the platform's keyless AI gateway (billed to platform credits) or the account's own OpenAI and ElevenLabs integrations when they exist. Users never need to bring an API key.

Packs that generate PDFs declare the `DOCUMENTS` capability and reuse the documents kernel — templates as repo files under `firebase/functions/documents/templates/<key>/`, generation through `documents__request__api` — never bespoke HTML-to-PDF plumbing. A new document type is a new template directory, not new code: see `docs/documents.md`. In the sandbox rendering uses the machine's own Chromium for free (`DOCUMENTS_MODE=local`); deploys use the platform's render service.

Packs (and upgrades) that need recurring work (cleanup, digests, rollups) declare the `JOBS` capability and register jobs in the jobs kernel's registry (`src/Jobs/JobsRegistry.ts`: name + cron expression + handler) — never hand-rolled timers or bespoke scheduler wiring: see `docs/jobs.md`. In the sandbox an in-process ticker runs due jobs for free (`JOBS_MODE=local`); deploys get one platform-provisioned Cloud Scheduler job ticking the environment every 5 minutes.

Packs that store user files (avatars, product images, attachments) declare the `STORAGE` capability and reuse the storage kernel — `createUpload`/`finalizeUpload`/`fileUrl`/`deleteUpload` plus `putUploadBytes` from `@base/core` — never bespoke bucket wiring or filesystem writes: see `docs/storage.md`. In the sandbox files live on local disk for free (`STORAGE_MODE=local`); deploys get a provisioned Cloud Storage bucket with V4 signed URLs.

Packs that grow multi-destination navigation reuse the kernel application shell (`AppShell` on web; the `NavigationShellView` twins on iOS/Android) — never a bespoke sidebar. Navigation is data against the shell nav schema (sections, items, badges, hotkeys, drill-down levels), so changing a product's IA is config edits mirrored across platforms: see `docs/shell.md`. The shell needs no capability — it is pure kernel chrome.

## Derived templates (remixes)

A derived template is a distinct entry in the platform's picker built from an existing pack rather than a new view tree: a thin `catalog.json` declaring `remixOf` (the base pack key), its own `key`/`templateKey`/`title`/`description`, a `contentSeed` (a content module that is a structural twin of the base pack's `content.ts`, copied over it at compose/switch time), and a `theme` brand overlay merged over the base's. Everything structural — `homeViewDir`, capabilities, landing routes, content contract — is inherited from the base; `scripts/verify-pack-catalogs.mjs` rejects a remix that redeclares any of it. The one exception is the landing register: a remix may carry one `landing` override, merged shallowly over the base's — a `style.preset` (its own register) and, optionally, its own `pages`, the pin that keeps a remix on the layout it shipped with when the base pack's home moves on. Because the merge is shallow, a pin replaces the base's pages wholesale: it must seed exactly the pages the base routes to, checked by the same seed gate as a base pack, and the base builders must emit exactly those section ids from the remix's seed (the base's landing fidelity test runs against the composed tree). That is how a base pack redesigns without dragging its remixes along: `services` wears `sitework` with its original remixes (`services-landscape`, `services-painting`, `services-hair`, `services-makeup`) inheriting its pages under their own brands, while `services-builder` pins `tideline` and the restyled skins pin their own registers (`crown` for `services-hair-braids`, `vanity` for `services-makeup-counter`, `riso` for `services-landscape-native`, `paintchip` for `services-painting-swiss`) and their section skeletons (the builders gate the ticker, build log, materials board, color deck, menu board, lookbook, and house rules on each seed's content, and move the metrics strip to the about page when the home leaves it empty); `services-recurring` wears `warm-boutique` with its original remixes (`services-lawncare`, `services-pest`) inheriting its pages, while `services-recurring-turnover`, `services-lawncare-crew` and `services-pest-creature` pin their own `memphis`, `gameday` and `creature` skeletons (the recurring builders pick the hero frame, the tickets, and the specimen board from each seed's content); `services-emergency` wears `sitework` with its original remixes (`services-electric`, `services-hvac`) inheriting its pages, while `services-emergency-van`, `services-electric-techno` and `services-hvac-desert` pin registers of their own (`jacaranda`, `schematic`, `sunbelt`) over skeletons chosen from the emergency builders' sections (a photograph sits beside the call unless a price board takes it full-bleed; an empty price board keeps the services grid and the price book on the home; a pin may re-variant a section, e.g. the electrician's hero as `full-bleed-media`); `care` wears `luxe-light` with its original remixes (`care-therapy`, `care-psychology`, `care-dental`) inheriting its pages under their own brands, while `care-direct`, `care-therapy-duet`, `care-psychology-studio`, and `care-dental-bright` pin `wayfinding`, `groove`, `inkblot`, and `bubblegum` over skeletons the care builders gate on each seed's content (a membership price list, a tracklist price list, inkblot plates, candy promise pills); `photography-family-documentary` pins `snapshot` over the pinboard home and scrapbook walls the family builders pick from its seed's `home.layout` while `photography-family` keeps the light-and-airy `heirloom` crossfade; the events family does the same — `gala` (`ballroom`), `reunion` (`picnic`), `vows` (`heirloom`), and `interiors` (`atelier`, shared by `interiors-architecture`, `interiors-cfo`, and `interiors-wedding`) carry the originals while `gala-disco`, `reunion-rodeo`, `vows-photobooth`, and `interiors-midcentury` pin `disco`, `rodeo`, `photobooth`, and `midcentury` over skeletons the base builders pick from each seed (`event.name` for the gala's invitation hero, `home.layout` for the reunion's poster, the vows zine, and the interiors catalog home); the content-driven builders (sections gated on content the skin seeds leave empty) keep their pixels unchanged. The base pack's landing modules keep their copy trade-neutral and pull the few trade-owned strings from the content module's `landingCopy` export, so a seed swap retrades the whole site. Seeds live beside the base content module (e.g. `web/app/src/View/Services/landscapeRemix.content.ts`) and a parity test per base pack (`tests/View/<Pack>/remixSeeds.test.ts`) pins each seed to the base module's export surface so CI catches contract drift. The six `services-*` trades above are the reference implementations.

### Wearing a remix: the composer contract

Every path that puts a derived template on a tree — `scripts/compose-pack.sh` staging a published template, the dev/studio switch (`scripts/lib/pack-switch.mjs` `switchPack`), and the **platform's agent-free template flip** — must apply the SAME four artifacts, all derived from `resolveCatalog(remix)` (the base catalog wearing the remix's identity, theme, landing register, and seed):

1. **`packs/active.json` = the BASE pack key** (`resolved.activeKey`), never the remix key — the kernel's routers (`homePageByPack`), the native `ActivePack` constants, and every document stamp only know real packs.
2. **Document overlays** (`applyPackOverlays`): the resolved `theme` merged over `repobot.theme.json`, the resolved `landing` over `repobot.landing.json`, the resolved `content` domains over `repobot.content.json` — each stamped `pack: <base key>`.
3. **The content seed copy**: the resolved catalog's `contentSeed` file copied byte-for-byte over the base pack's `contentContract.module` (e.g. `electricRemix.content.ts` → `View/ServicesEmergency/content.ts`). This step carries ALL of a remix's trade content — business identity, copy, service names, `landingCopy` headings. It is not optional and is not covered by step 2: the trade lives in the content MODULE, not in the documents, so a flip that stamps the documents but skips the seed copy ships the base pack's trade (an "Electrician" site reading as the base plumber) under the remix's brand.
4. **Platform identity from the remix** (`repobot.deploy.json` `templateKey`/`packKey`), where the composer emits one.

The safest implementation is to reuse the kernel's own: `resolveCatalog` + `applyPackOverlays` + the seed copy are exported by `scripts/lib/pack-switch.mjs` (dependency-free Node, present in every published tree), and `switchPack` performs the whole sequence including the pristine-restore bookkeeping a flip needs. Kernel gates pin the contract: `scripts/compose-pack.test.mjs` asserts a composed remix tree carries the seed bytes, the base `active.json`, and the remix deploy identity; `scripts/verify-pack-integrity.mjs` fails a remix whose base pack or seed module is missing, and an `active.json` naming a remix key.

## Pack palettes and the theme contract

Packs own their art direction, but the customer's `repobot.theme.json` always
wins. A pack's style file declares its palette as constants that read the
pack-overlay custom properties, with the pack's own art palette as the
fallback:

```ts
const accent = "var(--pack-accent, #d95d43)" // the pack's own art palette
const accentSoft = "var(--pack-accent-soft, #fbe9e4)"
const sans = "var(--pack-font, Sora, system-ui, sans-serif)"
```

The full set is `--pack-accent`, `--pack-accent-hover`, `--pack-accent-text`,
`--pack-accent-dark`, `--pack-accent-soft`, and `--pack-font`
(`packBrandVarNames` in the design system's `themeConfig.ts`). The properties
are declared only when the project actually customizes the theme
(`tokens.css.ts` bakes them; `themeHotUpdate.ts` re-declares them on live
edits), so fresh templates look exactly as designed while "make it my brand
color" flows into every pack — and, because the value is read at paint time
rather than baked into the compiled CSS, a live theme edit re-inks the pack
in the workspace preview with no rebuild. Never hardcode an accent directly
in a new pack, and never bake `packBrand?.accent ?? …` into a style file —
route it through the custom properties.

## Composing a template repo

`scripts/compose-pack.sh <pack-key> <output-dir>` stages the kernel with the pack applied: sets `active.json`, stamps the pack key into the native `ActivePack` constants (`ios/App/Config/ActivePack.swift`, `android/.../config/ActivePack.kt` — the iOS/Android home surfaces switch on it, mirroring the web `homePageByPack`), and emits a root `repobot.deploy.json` (capability manifest read by the deployer). A pack that ships its own `repobot.project.json` (project IA, `docs/project-ia.md`) gets it stamped to the root and scaffolded (`scripts/scaffold-ia.mjs`) so the template arrives with its marketing pages and dashboard destinations wired — the saas pack is the exemplar. The platform's publish pipeline runs this per pack and pushes each staged tree to its GitHub template repo.

Compose also stamps a generated **agent map** into the composed `AGENTS.md` (`scripts/generate-agent-map.mjs`): the marker-delimited `## Pack map` section deriving routes, sections, view files, and user-editable surfaces from the catalog and the pack tree, so a fresh template's first agent message works from a map instead of exploring the repo. It is never hand-written — `scripts/check-agent-maps.mjs` (check-all + CI) dry-runs the generator for every approved pack and standalone template and fails on maps that can't be produced or reference missing paths. A pack may add hand-authored guidance to the map through a designated `## Agent map notes` section in its `PACK.md`; the body merges verbatim, and any backticked repo path it cites is gated like the generated ones.

Agents upgrading a project (e.g. adding a backend to a game) should flip `clientOnly`/`capabilities` in `repobot.deploy.json`; the next deploy provisions the newly required infrastructure.
