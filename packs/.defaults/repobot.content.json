{
    "$comment": "Business-content contract. Owner-editable business facts, schema'd per domain (schedule today; listings, events, hours later), read by the ACTIVE pack with content.ts fallback (web/app/src/View/Landing/contentDocument.ts) — the business-facts sibling of repobot.landing.json. Compose stamps a schedule-bearing pack's seed here; the platform's Manage UI and the chat agent both edit this file, and invalid or missing values fall back to the pack's code content."
}
