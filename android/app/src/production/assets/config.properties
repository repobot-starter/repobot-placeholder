# Production flavor: stamped at build time by the Android build pipeline (see
# .github/workflows/android-build.yml). Empty values fail closed with a clear
# ConfigLoader error.
APP_NAME=
GRAPHQL_URL=
AUTH_MODE=builtin
AUTH_METHODS=email-code
AUTH_REDIRECT_URL=baseapp://auth/callback
