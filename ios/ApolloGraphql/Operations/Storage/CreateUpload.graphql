mutation CreateUpload($input: CreateUploadInput!) {
    createUpload(input: $input) {
        uploadId
        uploadUrl
        headersJson
        upload {
            id
            contentType
            sizeBytes
            visibility
            status
        }
    }
}
