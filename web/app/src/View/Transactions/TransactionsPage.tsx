/**
 * Entry point for the saas pack's `transactions` dashboard destination
 * (packs/saas/repobot.project.json) — see View/Overview/OverviewPage.tsx for
 * the pattern. The real view lives in View/Spend/.
 */
export { default } from "../Spend/TransactionsPage"
