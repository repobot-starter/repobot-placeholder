/**
 * Entry point for the credit pack's `credit-desk` dashboard destination
 * (packs/credit/repobot.project.json): the IA scaffolder wires routes to
 * View/<PascalCase(id)>/<PascalCase(id)>Page, and because this file already
 * exists it is kept as-is (docs/project-ia.md). The real view lives with the
 * rest of the credit surface in View/Credit/.
 */
export { default } from "../Credit/DeskPage"
