# Agent Guide

<!-- prettier-ignore-start -->
<!-- AGENT_MAP:v1 -->
## Pack map

Standalone SHOPIFY theme: **shopify-boutique**
(Shopify boutique). Generated by the kernel repo's
scripts/generate-agent-map.mjs from this template's tree — regenerate with
`node scripts/generate-agent-map.mjs --template shopify-boutique --write` there;
never hand-edit between the AGENT_MAP markers. The paths below are this
theme's whole surface — read them before exploring anywhere else.

### Page templates

| Template | Sections (id · type) |
| --- | --- |
| `templates/404.json` | main · main-404 |
| `templates/cart.json` | main · main-cart |
| `templates/collection.json` | main · main-collection |
| `templates/index.json` | hero · hero, featured · featured-collection, lookbook · lookbook, newsletter · newsletter |
| `templates/page.json` | main · main-page |
| `templates/product.json` | main · main-product |

Section order per page lives in those JSON templates (the theme editor
writes them); the section code lives in `sections/`.

### Sections and snippets

- `sections/announcement-bar.liquid`
- `sections/featured-collection.liquid`
- `sections/footer-group.json`
- `sections/footer.liquid`
- `sections/header-group.json`
- `sections/header.liquid`
- `sections/hero.liquid`
- `sections/lookbook.liquid`
- `sections/main-404.liquid`
- `sections/main-cart.liquid`
- `sections/main-collection.liquid`
- `sections/main-page.liquid`
- `sections/main-product.liquid`
- `sections/newsletter.liquid`
- `snippets/price.liquid` (snippet — `{% render %}`)

### User-editable surfaces

- Theme settings: `config/settings_schema.json` (the schema merchants see in
  the theme editor) and `config/settings_data.json` (current values)
- Strings: `locales/en.default.json` — every user-facing string resolves
  through the translation filter
- Styling: `assets/base.css` — the CSS custom properties declared at :root;
  wire new colors through the settings schema, never hardcode
- HTML shell: `layout/theme.liquid`
<!-- /AGENT_MAP -->
<!-- prettier-ignore-end -->

This repository is a **Shopify Online Store 2.0 theme** on repobot standards.
It is not a web-kernel project: there is no GraphQL layer, database, or
backend here — the storefront is rendered by Shopify from Liquid templates.

## What this store is

**Repobot Boutique** — an editorial apparel & accessories example store.
The home page composes announcement bar → hero → featured collection →
lookbook → newsletter; the product page adds size-guide and fabric-care
blocks. Demo copy lives in two places: merchant-editable text sits in
section `{% schema %}` setting/block defaults, and fixed chrome strings
(labels, buttons, empty states) live in `locales/en.default.json`. Keep the
voice confident, editorial, and concise when extending it.

## Repo map

| Path               | What it is                                                         |
| ------------------ | ------------------------------------------------------------------ |
| `layout/`          | The HTML shell (`theme.liquid`)                                    |
| `templates/*.json` | Page templates: which sections render, in what order               |
| `sections/`        | Section components (Liquid + `{% schema %}`), incl. section groups |
| `snippets/`        | Reusable Liquid partials (`{% render %}`)                          |
| `assets/`          | CSS / JS / images served by the Shopify CDN                        |
| `config/`          | Theme settings schema and current values                           |
| `locales/`         | Translations; every user-facing string goes through `              | t`  |
| `scripts/`         | Sandbox preview entry points (`repobot.sandbox.json`)              |
| `docs/`            | Standards, testing, and migration guides                           |

## Standards (the short version)

- Every user-visible string goes through the translation filter (`| t`) with
  a key in `locales/en.default.json`.
- New page-level building blocks are **sections** with a `{% schema %}` and a
  preset, so merchants can add them in the theme editor. Reusable fragments
  are **snippets**.
- Styling lives in `assets/base.css` on the CSS custom properties declared at
  `:root`; wire new colors through `config/settings_schema.json` rather than
  hardcoding.
- `npm run check` (Theme Check) must pass; CI runs it with
  `--fail-level error` on every push. Read `docs/standards.md` and
  `docs/testing.md` before larger changes.

## Live sandbox rules (Repobot workspace)

- With a connected Shopify store the preview is a live `shopify theme dev`
  session against the user's storefront; edits hot-reload.
- Never run `git` commands; the platform commits and pushes automatically.
- Write progress updates and summaries for the store's owner, not for an
  engineer — plain words about what is changing in their storefront, never
  internal mechanics like tests, refactors, or file names.
- You can create images with your image generation tool (banners, product
  placeholders, section backgrounds). When the user asks for an image,
  generate it and save it under `assets/` with a descriptive filename. The
  platform attaches images you create to your chat reply automatically, so
  don't apologize about not being able to show them.
- Theme publishing goes through the platform Deploy path (the dashboard's
  Deploy button, or `repobot deploy` when the CLI is available). Never run
  `npm run push` yourself: the store token is deliberately not in your
  environment, so it cannot work.

## Shopify store contract

- The store connection powers exactly two things: the live `shopify theme
dev` preview and platform theme deploys. It is theme-only.
- You cannot touch products, collections, inventory, orders, customers, or
  discounts directly — no Shopify Admin API credential exists in this
  workspace. Catalog changes go through the platform-action flow below.
- Never open or sign into the Shopify admin (admin.shopify.com,
  accounts.shopify.com) in a browser. Those pages are bot-protected and the
  attempt always fails.
- When the user asks for catalog changes (add or edit a product, change a
  price, upload product imagery): propose it as a platform action instead
  of saying you can't. Run `repobot actions list` to discover what the
  platform can do right now (the catalog is the source of truth — never
  enumerate capabilities from memory), then `repobot actions propose` to
  put a confirmation card in this chat. Nothing runs until the user
  confirms the card; on confirm the platform executes it through the
  store's Admin API connection and the card shows the result. If the CLI
  or a matching action is unavailable, fall back to an import-ready CSV in
  Shopify's product-import format, saved in the repo, with a pointer to
  Shopify admin -> Products -> Import. Once the product exists on the
  store, reference its real handle in theme code; the live preview renders
  the store's actual catalog.
