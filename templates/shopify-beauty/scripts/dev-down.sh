#!/usr/bin/env bash
# Sandbox teardown (referenced by repobot.sandbox.json). `shopify theme dev`
# and the fallback server both run in the foreground under the workspace
# supervisor, so there is nothing to stop here.
exit 0
